import random

names = []

n = int(input("Enter number of names: "))

for i in range(n):
    name = input("Enter name: ")
    names.append(name)

winner = random.choice(names)

print("Randomly selected name:", winner)
